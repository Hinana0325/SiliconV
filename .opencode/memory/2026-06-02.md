# 2026-06-02 — Cocoa GUI + Mac App Bundle

- User wanted SiliconV to follow "标准的 mac 软件架构" (standard Mac software architecture)
- Created Cocoa frontend (`frontend/cocoa/`):
  - `main.m` — NSApplication entry point
  - `AppDelegate.h` / `AppDelegate.m` — window, toolbar (kernel browse, start/stop), serial console NSTextView
  - `SiliconV-Info.plist` — bundle metadata (CFBundleIdentifier, version, icon)
  - `SiliconV.icns` — app icon (generated from SVG via sips + iconutil)
- Fixed CMake naming conflict: `siliconv` vs `SiliconV` collide on case-insensitive macOS FS → renamed CLI target to `sv-cli`
- CMake now produces two targets:
  - `SiliconV.app` — proper `.app` bundle with Icon, Info.plist, Cocoa GUI
  - `sv-cli` — CLI tool (same as old `siliconv`)
- App runs in "demo" mode on x86_64 (placeholder, shows guest config)
- Full ARM64 VM boot requires Apple Silicon or KVM host
- Updated `opencode.yml` build command
- Clean build: 0 errors, 0 warnings
- Added localization (en + zh-Hans): NSLocalizedString for all UI strings, .strings files
- Cocoa UI enhancements: NSToolbar, drag-drop, config popover, menus, status bar, rootfs
- Redesigned app icon (processor die + "SV" gold emblem + circuit traces)
- Structure optimization:
  - Flattened: `SiliconV/SiliconV/` → `SiliconV/` (eliminated double nesting)
  - Separated: agent config → `.opencode/` (AGENTS.md, SOUL.md, USER.md, TOOLS.md, memory/)
  - Clean root level: only project code + `.opencode/` + `.git/` + `.openclaw/`
